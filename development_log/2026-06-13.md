# 2026-06-13 开发日志

## 今日状态

当前项目已经从旧的单文件 `tcpepoll_01.cpp` 开始拆分为多个类：

1. `Socket`
2. `InetAddress`
3. `Epoll`
4. `Channel`
5. `TcpServer`
6. `TcpConnection`

## 已完成

1. `TcpServer.h` 和 `TcpConnection.h` 已添加 `#pragma once`。
2. `Channel` 已禁止拷贝。
3. `TcpConnection::tryWrite()` 已补充完整发送后的 `return true`。
4. `TcpConnection::refresh()` 已改为对端关闭后只关注剩余数据写出。
5. `Channel::handleEvent()` 已将 `EPOLLRDHUP` 从读事件判断中拆出，关闭事件由 close callback 处理。
6. `tcpepoll_02.cpp` 已改为创建 `TcpServer` 并调用 `start()`。

## 当前发现的问题

1. `Makefile` 仍指向不存在的 `tcpepoll.cpp`。
2. `Makefile` 的服务端源文件列表缺少 `TcpServer.cpp` 和 `TcpConnection.cpp`。
3. `TcpServer` 构造失败后没有初始化状态保护。
4. `TcpConnection` 中的 `server_` 暂时未使用，需要删除或明确用途。
5. `TcpServer::loop()` 仍然直接持有事件循环逻辑，后续应抽象 `EventLoop`。

## 下一步

优先完成阶段 0：稳定当前单线程 echo server。不要急着上多线程。
